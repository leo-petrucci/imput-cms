{
  "name": "meow-cms-monorepo",
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "docs:build": "turbo run docs:build",
    "build": "turbo run meow:build",
    "release": "turbo run meow:release",
    "dev": "turbo run docs:dev"
  },
  "devDependencies": {
    "@meow/components": "workspace:*",
    "local-meow-cms": "workspace:*",
    "prettier": "latest",
    "process": "^0.11.10",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "packageManager": "pnpm@8.9.0"
}
